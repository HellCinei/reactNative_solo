import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	container: {
		backgroundColor: '#fff',
		flexDirection: "row",
		height: 60,
		width: '100%',
		borderTopWidth: 1,
		borderTopColor: '#ddd',
		alignItems: "center",
		justifyContent: "space-around",
	},

	txtIcon: {
		textAlign: "center",
		fontSize: 11,
		fontWeight: "bold",
	},
});